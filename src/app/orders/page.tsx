import UserOrders from "@/components/orders/UserOrders";

export default function orderPage() {
    return (
        <main className="relative items-center justify-between min-h-screen">
            <UserOrders/>
        </main>
    )
}